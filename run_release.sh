#!/usr/bin/env bash

# version 2

PRODUCT_VERSION="${PRODUCT_VERSION:-unknown_version}"
PRODUCT_ENV="${PRODUCT_ENV:-unknown_environment}"

echo "The new version ${PRODUCT_VERSION} of my product is released at ${PRODUCT_ENVIRONMENT}"
